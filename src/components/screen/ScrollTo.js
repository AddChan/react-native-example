import React from "react";
import { View, Text } from "react-native";

const ScrollTo = () => {
  return (
    <View>
      <Text> ScrollToScreen </Text>
    </View>
  );
};

export default ScrollTo;
